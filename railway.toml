[build]
builder = "DOCKERFILE"
dockerfilePath = "kmo-driver/Dockerfile"

[deploy]
healthcheckPath = "/api/health"
healthcheckTimeout = 100
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
